load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "mkbom",
    srcs = ["src/main.rs"],
    visibility = ["//visibility:public"],
    deps = [
        "//crates/xcbuild-bom",
        "@crates//:anyhow",
        "@crates//:walkdir",
    ],
)
